name: lupusweb
recipe: laravel
config:
  php: '8.0'
  webroot: public
  via: nginx
  database: mariadb
  xdebug: true
  password: postgres
services:
  database:
    portforward: 3307
  node:
    type: node:14
    globals:
      npm: 7
tooling:
  npm:
    service: node
  npx:
    service: node
